export const numberValidationPattern = {
	value: /^[0-9]*$/,
	message: 'Must be a number',
};

export const contactNumValidationPattern = {
	value: /^[0-9]*$/,
	message: 'Should only contain numbers',
};
